import { StyleSheet, Text, View } from 'react-native';

const ProfileScreen = () => {
  return (
    <View className="w-full h-screen justify-center items-center">
      <Text className="text-4xl">ProfileScreen</Text>
    </View>
  );
};

export default ProfileScreen;
